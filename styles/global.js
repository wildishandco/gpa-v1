import { css } from "styled-components";

const global = css`
  html {
    width: 100%;
    overflow-x: hidden;
  }

  body {
    font-family: cooper-black-std, serif;
    font-style: normal;
    font-weight: 400;
  }
`;

export default global;
